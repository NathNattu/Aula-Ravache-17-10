<!DOCTYPE html>
<html lang="PT-BR">
<head>
    <meta charset ="utf=8">
    <title>Pokédex</title>
</head>
<body>
    <h1>Pokédex</h1>
    <ul>
        <% pokemons.forEach(function(pokemon)){%>
        <li>
            <a href="/pokemon/<%= pokemon.id %>"<%= pokemon.nome%>  
    </a> -  <% = pokemon.tipo%>
        </li>
        <% }); %>
    </ul>
</body>
</html>